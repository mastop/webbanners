{% extends '::frontend.html.twig' %}

{% block content %}
<div class="span12">
    <div class="span7">
        <h1>{{ title }}</h1>
        <form class="block" action="{{ path('core_html_contact') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
                <div><button class="btn btn-success"><span>Enviar</span></button> <button class="btn" href="#" onclick="javascript:history.go(-1); return false;">Cancelar</button></div>
        </form>
    </div>
    <div class="span4">
    {% render 'BannerCoreBundle:Widget\\Block:render' %}
    </div>
</div>
<div class='clear'></div>
{% endblock content %}  